# Native tools

Generated and ignored by Git. `scripts/prepare_tools.py` downloads a pinned official Node distribution and verified FFmpeg/LAME source archives. `scripts/build_ffmpeg.sh` builds the minimal audio FFmpeg helper for the host architecture. Do not commit binaries. See docs/RELEASING.md.
